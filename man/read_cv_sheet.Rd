% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_cv_sheet.R
\name{read_cv_sheet}
\alias{read_cv_sheet}
\title{Read a Specific Sheet from a Google Sheet}
\usage{
read_cv_sheet(
  doc_identifier,
  sheet_name,
  na_strings = c("", "NA", "N/A"),
  col_types = NULL,
  trim_ws = TRUE
)
}
\arguments{
\item{doc_identifier}{A character string representing the ID, URL, or a unique
name of the target Google Sheets document.}

\item{sheet_name}{A character string of the exact sheet name (tab) to read.}

\item{na_strings}{A character vector of strings to be interpreted as \code{NA}.
Defaults to \code{c("", "NA", "N/A")}.}

\item{col_types}{A character string specifying the column types for \code{googlesheets4}.
Defaults to \code{NULL} for auto-detection.}

\item{trim_ws}{A logical value. If \code{TRUE} (the default), leading and trailing
whitespace is trimmed from character cells.}
}
\value{
A \code{tibble} containing the data from the specified sheet.
}
\description{
Reads a single sheet from a Google Sheet document, handling identifier
resolution, data reading, and post-read checks.
}
\details{
This function serves as the core data reading engine. It takes a
flexible \code{doc_identifier} (which can be a URL, a sheet ID, or a document
name), resolves it to a stable identifier, reads the specified \code{sheet_name},
and returns the data as a tibble.
}
